import React from 'react';
import { TodoListItemWrapper } from './TodoListItem.style';

const TodoListItem = () => {
  return <TodoListItemWrapper>todolistitem</TodoListItemWrapper>;
};

export default TodoListItem;
