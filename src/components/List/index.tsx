import React from 'react';
import ListItem from '../ListItem';
import { ListWrapper } from './List.style';

const List = () => {
  return (
    <ListWrapper>
      <ListItem />
    </ListWrapper>
  );
};

export default List;
